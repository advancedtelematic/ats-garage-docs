# Site title and subtitle. This is used in _includes/header.html
title: 'Documentation'
subtitle: 'ATS Garage'

# if you wish to integrate disqus on pages set your shortname here
disqus_shortname: ''

# if you use google analytics, add your tracking id here
google_analytics_id: 'UA-69301419-1'

# Enable/show navigation. There are there options:
#   0 - always hide
#   1 - always show
#   2 - show only if posts are present
navigation: 2

# URL to source code, used in _includes/footer.html
codeurl: 'https://app.atsgarage.com/'

# Categories that can appear in the navigation
sections: [
    ['start-yocto', 'Getting started with Yocto'],
    ['start-garage', 'Getting started with the garage'],
    ['bas', 'Basics'],
    ['feat', 'Features'],
    ['new', 'News'],
    ['ref', 'Reference'],
    ['ostree', 'OSTree'],
    ['faq', 'Questions'],
    ['hidden', '']
    #['post', 'Posts']
]

# This creates navigation tabs. First element in the array is the name of the tab,
# second is the id of the tab's div, third is the list of sections that should
# appear in the tab
# 
# Note that any sections which aren't assigned to any navigation tab will not show 
# up in navigation anywhere
navigation_tabs: [
    ['Learn', 'learn', ['bas', 'start-yocto', 'start-garage', 'ref']],
    ['OSTree', 'ostree', ['ostree']],
    ['FAQ', 'faq', ['faq']]
]

# Links that should appear as top-bar navigation items. Docs should always be
# the last one, because of a stupid hack.
header_links: [
    ['How it works', 'http://ats-garage.ventureoak.com/en/how-it-works.html'],
    ['Pricing', 'http://ats-garage.ventureoak.com/en/pricing.html'],
    ['News', 'http://ats-garage.ventureoak.com/en/news.html'],
    ['FAQ', 'http://ats-garage.ventureoak.com/en/FAQ.html'],
    ['Contact', 'http://ats-garage.ventureoak.com/en/contact.html'],
    ['Docs', '/']
]

# Keep as an empty string if served up at the root. If served up at a specific
# path (e.g. on GitHub pages) leave off the trailing slash, e.g. /my-project
baseurl: '' 

# Dates are not included in permalinks
permalink: none

# Syntax highlighting
highlighter: pygments

# Since these are pages, it doesn't really matter
future: true

# Exclude non-site files
exclude: ['bin', 'README.md','README.adoc','Gemfile*','Makefile','Dockerfile','publish-docs.sh','design']

gems: 
  - jekyll-asciidoc
  - octopress-debugger

asciidoctor:
  attributes:
    - hardbreaks!
    - source-highlighter=pygments
    - pygments-css=style


